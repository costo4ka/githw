apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-log-reader
  labels:
    {{- include "test-repo-chart.labels" . | nindent 4 }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ .Release.Name }}-pod-log-reader
  labels:
    {{- include "test-repo-chart.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["pods", "pods/log"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ .Release.Name }}-log-reader-binding
  labels:
    {{- include "test-repo-chart.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ .Release.Name }}-log-reader
roleRef:
  kind: Role
  name: {{ .Release.Name }}-pod-log-reader
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-web
  labels:
    {{- include "test-repo-chart.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.web.replicas }}
  selector:
    matchLabels:
      app: test-repo-web
  template:
    metadata:
      labels:
        app: test-repo-web
    spec:
      serviceAccountName: {{ .Release.Name }}-log-reader
      initContainers:
        - name: fetch-logs
          image: bitnami/kubectl:latest
          command: ["/bin/sh", "-c"]
          args:
            - |
              echo "Waiting for app pod to be ready..."
              kubectl wait --for=condition=ready pod -l app=test-repo-app --timeout=120s
              sleep 5
              kubectl logs -l app=test-repo-app --tail=-1 > /shared/index.html 2>&1
          volumeMounts:
            - name: html
              mountPath: /shared
      containers:
        - name: web
          image: {{ .Values.web.image }}
          command: ["nginx", "-g", "daemon off;"]
          ports:
            - containerPort: 80
          volumeMounts:
            - name: html
              mountPath: /usr/share/nginx/html
      volumes:
        - name: html
          emptyDir: {}
